# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native build output (continuous native generation)
ios/
android/

# Source lib layer: re-included from the root `lib/` build-output ignore so
# the transport/API sources stay trackable (an ignored src/lib was invisible
# to git and any ignored-file cleanup wiped it silently).
!src/lib/

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
